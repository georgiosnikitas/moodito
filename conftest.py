"""Pytest configuration.

The presence of this file at the repository root makes pytest add the root to
``sys.path``, so tests can ``import app`` and ``import emotion`` regardless of
how pytest is invoked (bare ``pytest`` or ``python -m pytest``).
"""
